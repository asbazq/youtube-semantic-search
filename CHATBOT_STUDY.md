# 로컬 자막 챗봇 학습 가이드

이 문서는 이 프로젝트에 추가한 챗봇을 읽으며 **체인, 구조화 출력, 메모리,
Tool Binding, 안정성, 복합 체인, RAG**를 복습하기 위한 안내서다.
현재 실행 경로는 일반 Python 함수와 Ollama HTTP API로 구성되어 있다.
**LangChain `Runnable`, Agent, LangGraph는 아직 사용하지 않는다.** 따라서
아래 ①~⑦은 해당 *개념*을 코드에 적용한 것이며, 수업에서 본 LangChain API와
일대일로 같은 구현이라고 이해하면 안 된다. ⑧·⑨는 선택 확장 과제다.

## 먼저 전체 흐름 보기

```text
Vue 질문 + session_id + 선택한 video_id
               │
               ▼
POST /api/chat ── ChatRequest 입력 검증
               │
               ▼
SessionMemory에서 최근 대화 읽기
               │
               ▼
로컬 LLM에 search_transcripts 도구 설명 전달
               │
               ▼
모델이 제안한 검색어 검사 → 기존 자막 검색기 실행
               │
               ▼
유사도 기준 통과 자막 최대 4개에 [1]~[4] 번호 부여
               │
               ▼
로컬 LLM에 질문 + 자막 전달 → JSON 답변 검증
               │
               ▼
답변 저장 → 답변과 실제 영상 시점 링크 반환
```

자막 수집·청크 생성·임베딩·ChromaDB 저장은 기존 영상 등록 파이프라인이
담당한다. 이 중 임베딩은 `SentenceTransformer`로 로컬에서 계산한다.
챗봇이 쓰는 검색 결과는 기존 [`search/semantic_search.py`](search/semantic_search.py)의
`search()` 또는 `search_by_video()`에서 나온다. 답변 생성만 로컬 Ollama
모델을 추가로 사용한다. **모델을 새로 학습하거나 파인튜닝한 코드는 아니다.**
새 YouTube 자막을 가져올 때에는 네트워크가 필요할 수 있지만, 등록된 자막에
대한 검색·답변 생성에는 외부 생성 AI API 키를 사용하지 않는다.

## 코드에서 패턴 찾기

| 패턴 | 위치와 역할 | 직접 확인할 점 |
|---|---|---|
| ① 체인 만들기 | [`chatbot/service.py`](chatbot/service.py)의 `ChatService.ask()`가 단계의 입력과 출력을 연결 | 각 단계의 결과가 다음 단계의 어떤 인자로 들어가는가? |
| ② 구조화 출력 | `ANSWER_SCHEMA`를 Ollama `format`에 전달한 뒤 `json.loads()`와 출처 번호 검증 | JSON 문법이 맞아도 출처 번호가 틀리면 왜 거부하는가? |
| ③ 대화 메모리 | `SessionMemory`가 `(session_id, video_id)`별 최근 질문·답변을 저장 | 다른 세션·영상으로 바꾸면 이전 대화가 섞이는가? |
| ④ Tool Binding | `SEARCH_TOOL`을 Ollama `tools`에 전달하고 `_tool_query()`에서 호출 인자를 읽음 | 모델이 도구를 *제안*하는 것과 서버가 도구를 *실행*하는 것은 어떻게 다른가? |
| ⑤ 안정성 | [`chatbot/ollama_client.py`](chatbot/ollama_client.py)의 일시 오류 재시도, 근거 부족 시 생성 생략, JSON 형식 재시도 | 어떤 오류는 재시도하고 어떤 오류는 즉시 중단하는가? |
| ⑥ 복합 체인 | 검색어 선택 → 범위별 검색 → 근거 선별 → 답변 생성 → 기록 저장 | 한 단계 실패가 뒤 단계에 어떤 영향을 주는가? |
| ⑦ RAG | ChromaDB 검색 결과의 자막을 생성 모델의 프롬프트에 넣음 | 답변의 근거는 모델의 기억인가, 검색된 자막인가? |
| ⑧ Agent | 미적용 | 여러 도구 중 선택하고 반복 호출해야 할 기능이 생기면 고려 |
| ⑨ LangGraph | 미적용 | 검색 결과 평가·재검색·사용자 확인 같은 명시적 분기가 생기면 고려 |

### ①·④ 검색어 선택과 도구 실행

`SEARCH_TOOL`은 `search_transcripts(query: str)`라는 도구의 이름·설명·인자
형식이다. 첫 번째 모델 호출은 최근 대화를 참고해 검색어를 도구 호출 형태로
제안할 수 있다. 예를 들어 이전 질문이 “데드리프트 허리 자세는?”이고 현재
질문이 “그다음은?”이면 “데드리프트 허리 자세 다음 설명”처럼 단독으로 검색
가능한 문장을 만들도록 요청한다.

여기서 **모델은 ChromaDB에 직접 접근하지 않는다.** `ChatService`가 모델의
`tool_calls`에서 허용된 도구 이름과 `query` 인자만 읽어 기존 검색기의 Python
메서드를 호출한다. 모델이 도구 호출을 생략하거나 잘못된 인자를 주면 질문
원문, 또는 직전 질문과 현재 질문을 합친 문장으로 검색한다. 작은 로컬 모델의
도구 호출 능력에 기능 전체가 의존하지 않게 한 폴백이다.

영상 선택 값 `video_id`는 브라우저 요청에서 가져오며 모델의 도구 인자로
받지 않는다. 따라서 특정 영상을 선택했을 때 모델이 검색 범위를 임의로
다른 영상으로 바꾸지 못한다.

### ②·⑦ 근거를 붙인 구조화 답변

검색된 자막에는 서버가 `[1]`, `[2]`처럼 번호를 붙인다. 두 번째 모델 호출은
질문과 이 자막을 보고 아래 모양의 JSON을 만든다. 값은 **형식 예시**다.

```json
{"answer": "자막에서는 허리를 중립으로 유지하라고 설명합니다.", "citations": [1]}
```

`format=ANSWER_SCHEMA`가 JSON 형식을 유도해도 결과를 그대로 믿지 않는다.
서버는 `answer`가 비어 있지 않은 문자열인지, `citations`가 실제 검색 결과의
번호 범위 안에 있는 정수 배열인지 검사한다. 잘못되면 모델에 한 번 수정 요청하고,
두 번 모두 실패하면 HTTP 502를 반환한다. 출처 번호가 비어 있으면 근거가
없는 답변으로 보고 안내 문구로 바꾼다.

영상 제목·시점·URL은 **모델 출력이 아니라 검색 결과 원본**에서 가져온다.
화면은 모델이 고른 출처 번호와 별도로 검색된 영상 구간 최대 4개를 보여 준다.
따라서 화면의 링크 목록은 “검색 결과”이며, 모두가 모델 답변에서 인용된
것을 뜻하지는 않는다. 모델의 답변이 자막의 사실과 맞는지도 직접 확인해야 한다.

현재 검색기는 유사도가 낮아도 후보 최대 3개를 반환할 수 있다. 챗봇은
`CHAT_MIN_SCORE`(기본 0.45)보다 낮은 후보를 다시 제외한다. 이 점수는
정답 확률이 아니다. 기존 검색은 의미 검색 유사도와 짧은 키워드의 유사도
점수를 함께 사용하므로 기준값은 실제 질문으로 평가하고 조정해야 한다.

### ③ 메모리와 ⑤ 안정성

`SessionMemory`는 최근 4턴의 질문·답변만 보관한다. 키는 `(session_id,
video_id)`이므로 같은 브라우저 대화라도 영상 범위를 바꾸면 다른 기록을
사용한다. 서버 전체에서는 마지막으로 사용한 최대 200개의 세션·영상 범위
조합을 보관하고
오래 사용하지 않은 항목부터 버린다. 데이터베이스에 저장하지 않으므로
서버를 재시작하거나 worker를 여러 개 띄우면 대화가 유지·공유되지 않는다.

Ollama 호출은 시간 초과나 일시적인 HTTP 오류에서 최대 한 번 재시도한다.
모델 미설치(404)는 재시도로 해결되지 않아 즉시 HTTP 503 안내로 바꾼다.
검색 오류는 500, 모델 답변 JSON 오류는 502로 구분한다. 검색된 자막이
없거나 기준 점수보다 낮으면 **생성 모델을 호출하지 않고** 근거가 없다고
답한다. 다른 모델로 자동 전환하는 폴백은 아직 없다.

## 요청과 응답 살펴보기

FastAPI의 `POST /api/chat`에 보내는 요청 예시다. `session_id`는 최소 8자여야
하며 Vue 화면에서는 브라우저가 UUID를 생성한다.

```json
{"question": "데드리프트에서 허리를 어떻게 유지하나요?", "session_id": "study-0001", "video_id": null}
```

응답에는 `answer`, `sources`, `citations`, `search_query`, `session_id`가 있다.
`sources`의 각 항목에는 기존 검색 결과의 `text`, `video_title`,
`start_time`, `score`, `youtube_url` 등이 들어간다. `search_query`를 보면
모델이 검색어를 바꿨는지, 폴백이 사용됐는지 비교할 수 있다.

## 실행과 실습 순서

Docker Compose를 사용할 경우:

```bash
docker compose up -d --build
docker compose exec ollama ollama pull qwen2.5:1.5b
```

PC에 Ollama를 직접 실행한다면 `OLLAMA_BASE_URL` 기본값은
`http://127.0.0.1:11434`다. Docker 안의 백엔드는 서비스 이름을 사용하므로
`docker-compose.yml`에서 `http://ollama:11434`로 설정한다. 모델을 바꿀 때는
`OLLAMA_MODEL`을 설정하고 같은 모델을 내려받는다. 저장된 자막의 임베딩
모델을 바꾸지 않는다면 기존 ChromaDB 인덱스는 재사용할 수 있다.

다음 순서로 각각 하나의 개념을 관찰할 수 있다.

1. 영상을 등록하고 검색 화면에서 찾을 수 있는 자막 내용으로 질문한다.
   답변과 영상 시점 링크가 실제 자막에 맞는지 확인한다.
2. 같은 대화에서 “그다음은?”이라고 묻는다. `search_query`가 궁금하면
   브라우저 개발자 도구의 `/api/chat` 응답을 본다.
3. 영상 하나를 선택해 같은 질문을 반복한다. 전체 영상 검색과 출처가
   어떻게 달라지는지 비교한다.
4. **새 대화**를 누르거나 영상 범위를 바꾼 뒤 이전 내용을 가리키는
   질문을 해 본다. 세션 메모리의 경계를 확인할 수 있다.
5. `CHAT_MIN_SCORE`를 높이거나 무관한 질문을 해 근거 부족 폴백을 확인한다.
6. 학습용 단위 테스트를 실행한다. 실제 Ollama 서버 없이 도구 호출,
   점수 필터, 잘못된 출처 번호, 메모리 분리를 검증한다.

```bash
python -m unittest discover -s tests -p test_chat_service.py -v
cd frontend && npm run build
```

## 다음 확장에 대한 판단

현재 질문은 “자막 검색 한 번 → 답변 한 번”이라는 고정된 흐름이다. 그래서
Agent나 LangGraph를 지금 추가하면 같은 일을 더 복잡하게 표현하게 된다.
여러 영상 요약·비교, 검색어 재작성 후 재검색, 근거 평가 후 답변 보류처럼
**실제로 선택과 반복이 필요한 요구**가 생기면 확장해 볼 수 있다.

- **Agent 실습:** `search_transcripts`, `list_videos`, `compare_videos` 등
  서로 다른 도구를 준비하고 모델이 어떤 도구를 언제 선택하는지 기록한다.
- **LangGraph 실습:** `plan → retrieve → grade → answer` 노드와
  `grade` 결과에 따른 재검색/종료 분기를 만든다. 반복 횟수 상한을 둔다.
- **LangChain 실습:** 현재 `ChatService.ask()`의 단계들을 `Runnable`로
  옮겨 같은 입력·출력과 실패 동작이 유지되는지 비교한다.

실습용 `qwen2.5:1.5b`는 작은 모델이므로 도구 호출 또는 JSON 출력이
항상 성공한다고 가정하면 안 된다. 단위 테스트는 폴백과 검증 로직을
확인하지만 실제 모델의 답변 품질이나 Ollama 연결까지 보증하지 않는다.
